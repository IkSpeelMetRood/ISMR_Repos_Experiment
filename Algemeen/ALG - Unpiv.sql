SELECT
	STUDENTID,
	[SUBJECTNAMES],
	MARKS
FROM
	(
	SELECT
		STUDENTID,
		MATH,
		SCIENCE,
		ENGLISH
	FROM
		STUDENT
) AS S
UNPIVOT
(
    MARKS FOR [SUBJECTNAMES] IN (MATH, SCIENCE, ENGLISH)
) AS UNPVT;